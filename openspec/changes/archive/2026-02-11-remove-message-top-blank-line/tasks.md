## 1. メッセージ項目マークアップの整理

- [x] 1.1 `frontend/src/components/MessageList.tsx` の通常表示でメニュー専用ヘッダー行を廃止し、本文前に専用空行を作らない構造へ変更する。
- [x] 1.2 `...` トリガーの表示位置を維持しつつ、編集・削除メニューの開閉と既存ハンドラー連携（編集開始/削除確認）を維持する。
- [x] 1.3 非編集モードと編集モードの分岐を見直し、編集 UI（textarea・保存・キャンセル・エラー表示）の既存挙動を維持する。

## 2. スタイル調整とレスポンシブ確認

- [x] 2.1 `frontend/src/App.css` の `message-item` / `message-menu` 周辺スタイルを更新し、通常表示時に本文上部の不要な空行を解消する。
- [x] 2.2 モバイル幅（例: 375px）と PC 幅（例: 1280px）で本文可読性とメニュー操作性が保たれるよう余白・配置を調整する。

## 3. テスト更新と回帰確認

- [x] 3.1 `frontend/src/components/MessageList.test.tsx` を更新し、通常表示で本文前に専用空行を生む構造が存在しないことを検証する。
- [x] 3.2 `frontend/src/App.test.tsx` を更新し、レイアウト変更後も編集/削除メニューの操作フローが維持されることを検証する。
- [x] 3.3 `frontend` のテストを実行し、今回の変更による表示・操作回帰がないことを確認する。
