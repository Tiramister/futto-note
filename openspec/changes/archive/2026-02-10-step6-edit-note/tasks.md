## 1. バックエンド: メッセージ更新 API

- [x] 1.1 認証ミドルウェア配下に `PUT /api/messages/{id}` ルートとハンドラーを追加し、`id` と `body` の入力検証（400）を実装する。
- [x] 1.2 `messages` テーブルに対して `id` と `user_id` を条件に更新する SQL を実装し、対象なしを 404、成功時に更新済みメッセージを返却する。
- [x] 1.3 更新 API の 200/400/401/404 ケースを `backend/messages_test.go` で検証する。

## 2. フロントエンド: 編集 UI と更新処理

- [x] 2.1 メッセージ項目に操作メニュー（`...`）と「編集」導線を追加し、インライン編集モード（textarea + 保存/キャンセル）を実装する。
- [x] 2.2 `App.tsx` と `useMessages.ts` に編集状態管理と `PUT /api/messages/:id` 呼び出しを追加し、保存成功時に対象メッセージを置換する。
- [x] 2.3 保存失敗時のエラー表示、キャンセル時の表示復元、保存中の重複操作防止を実装する。

## 3. スタイル調整と検証

- [x] 3.1 編集メニューとインライン編集フォームがスマホ/PC で崩れないよう `App.css` を更新する。
- [x] 3.2 フロントエンドテストで「編集開始」「保存成功」「キャンセル時 API 未実行」を検証する。
- [x] 3.3 `go test ./...` と `npm run test` で主要動作を確認する。
