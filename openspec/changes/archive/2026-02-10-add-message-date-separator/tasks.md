## 1. 日付セパレーター判定ロジックの追加

- [x] 1.1 メッセージ配列を走査し、日付グループ先頭を判定するロジックをタイムライン描画層に追加する。
- [x] 1.2 先頭メッセージまたは日付変更時のみセパレーターを挿入し、欠損日を補間しないことを確認する。
- [x] 1.3 セパレーター用日付文字列を固定 `yyyy/MM/dd` で生成するフォーマット関数を実装する。

## 2. タイムライン UI への反映

- [x] 2.1 セパレーター行（横線 + 日付文字列）コンポーネントまたは描画ブロックを追加する。
- [x] 2.2 セパレーター表示が既存メッセージ本文・投稿時刻表示を壊さないようにマークアップとスタイルを調整する。
- [x] 2.3 セパレーターの日付判定と表示フォーマットが既存時刻表示と同じタイムゾーン基準になるよう統一する。

## 3. 表示仕様テストの追加

- [x] 3.1 日付境界でのみセパレーターが表示されるケース（異日付連続メッセージ）をテストで追加する。
- [x] 3.2 同一日付内でセパレーターが重複しないケースをテストで追加する。
- [x] 3.3 メッセージのない日付を補間表示しないケース（例: 1/16→1/18）をテストで追加する。
- [x] 3.4 ロケールが異なっても `yyyy/MM/dd` 固定表示になるケースをテストで追加する。
