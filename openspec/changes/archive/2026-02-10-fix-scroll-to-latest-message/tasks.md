## 1. スクロール制御基盤の整理

- [x] 1.1 `MessageList` で最新メッセージ要素を参照できるようにし、スクロール対象を要素ベースで特定できる状態にする。
- [x] 1.2 `useMessages` に初回表示用・送信成功用のスクロール要求状態を追加する。
- [x] 1.3 描画完了後にスクロール要求を 1 回だけ消費して最新メッセージへ移動する共通処理を実装する。

## 2. 送信フローとの統合

- [x] 2.1 `App.tsx` の送信成功処理から `setTimeout` 依存を除去し、メッセージ追加とスクロール要求を同一フローで扱う。
- [x] 2.2 初回メッセージ取得完了時にのみ自動スクロールを実行し、不要な再スクロールを防ぐ。
- [x] 2.3 ログアウト・再ログイン時にスクロール関連状態が正しくリセットされることを確認する。

## 3. テストの更新

- [x] 3.1 初回表示時に最新メッセージが表示対象になることを検証するテストを追加または更新する。
- [x] 3.2 送信成功時に追加された新規メッセージまでスクロールすることを検証するテストを追加または更新する。
- [x] 3.3 既存のメッセージ表示・送信系テストが回帰しないことを確認する。

## 4. 動作確認と仕上げ

- [x] 4.1 `frontend` テストを実行し、スクロール関連変更を含めて全件成功を確認する。
- [x] 4.2 PC 幅・スマホ幅で初回表示と送信後スクロールを手動確認する。
- [x] 4.3 OpenSpec artifact（proposal/design/specs/tasks）の整合性を最終確認する。
