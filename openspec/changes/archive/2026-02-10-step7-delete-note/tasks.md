## 1. バックエンド: メッセージ削除 API

- [x] 1.1 認証ミドルウェア配下に `DELETE /api/messages/{id}` ルートとハンドラーを追加し、`id` の入力検証（400）を実装する。
- [x] 1.2 `messages` テーブルに対して `id` と `user_id` を条件に削除する SQL を実装し、対象なしを 404、成功時に 204 No Content を返す。
- [x] 1.3 削除 API の 204/400/401/404 ケースを `backend/messages_test.go` で検証する。

## 2. フロントエンド: 削除導線と確認フロー

- [x] 2.1 `MessageList` の操作メニューに「削除」を追加し、選択時に確認ダイアログ（`このメッセージを削除しますか？`）を表示する。
- [x] 2.2 `App.tsx` に削除処理を追加し、確認 OK 時のみ `DELETE /api/messages/:id` を呼び出す。
- [x] 2.3 削除成功時に対象メッセージを一覧から除去し、キャンセル時は API 未実行・表示不変を保証する。

## 3. テストと動作確認

- [x] 3.1 フロントエンドテストで「削除確認ダイアログ表示」「OK で API 実行・一覧反映」「キャンセル時 API 未実行」を検証する。
- [x] 3.2 削除失敗時に対象メッセージが残ることと、再試行可能なエラー表示を検証する。
- [x] 3.3 `go test ./...` と `npm run test` を実行して主要動作を確認する。
