## 1. バックエンド: 認証基盤

- [x] 1.1 セッショントークン生成ユーティリティを実装する（crypto/rand で 32 バイト、hex エンコード）
- [x] 1.2 認証ミドルウェアを実装する（Cookie からトークン取得 → sessions 検索 → context に user_id 埋め込み）

## 2. バックエンド: 認証 API

- [x] 2.1 `POST /api/login` ハンドラーを実装する（bcrypt でパスワード照合、セッション作成、Cookie 設定）
- [x] 2.2 `POST /api/logout` ハンドラーを実装する（セッション削除、Cookie 無効化）
- [x] 2.3 `GET /api/me` ハンドラーを実装する（セッション検証、ユーザー情報返却）
- [x] 2.4 ルーターに認証エンドポイントを登録する

## 3. フロントエンド: ログイン画面

- [x] 3.1 ログインフォームコンポーネントを作成する（ユーザー名・パスワード入力欄、ログインボタン）
- [x] 3.2 フォーム送信処理を実装する（`/api/login` への POST、エラーハンドリング）

## 4. フロントエンド: 認証状態管理

- [x] 4.1 認証状態を管理する仕組みを実装する（useState で user 情報を保持）
- [x] 4.2 起動時の認証確認を実装する（`/api/me` を呼び出し、結果に応じて画面を切り替え）
- [x] 4.3 認証状態に応じた画面切り替えを実装する（未認証: ログイン画面、認証済み: メイン画面）

## 5. フロントエンド: ログアウト

- [x] 5.1 ログアウトボタンをメイン画面に追加する
- [x] 5.2 ログアウト処理を実装する（`/api/logout` への POST、認証状態クリア、ログイン画面へ遷移）

## 6. 統合テスト

- [x] 6.1 `create_user.go` で作成したユーザーでログインできることを確認する
- [x] 6.2 ログイン後にページをリロードしてもセッションが維持されることを確認する
- [x] 6.3 ログアウト後に `/api/me` が 401 を返すことを確認する
