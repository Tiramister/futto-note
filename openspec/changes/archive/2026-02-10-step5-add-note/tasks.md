## 1. バックエンド: メッセージ作成 API

- [x] 1.1 認証ミドルウェア配下に `POST /api/messages` ルートとハンドラーを追加する。
- [x] 1.2 リクエストボディから `body` を取得し、空文字を 400 で拒否する入力検証を実装する。
- [x] 1.3 `messages` テーブルへ `INSERT ... RETURNING id, body, created_at` を実装し、201 で作成結果を返却する。
- [x] 1.4 未認証アクセスが 401 を返し、作成済みデータがリクエストユーザーに紐づくことを確認する。

## 2. フロントエンド: 入力エリアと送信操作

- [x] 2.1 タイムライン下部に固定配置の `textarea` と送信ボタンを配置し、入力状態を管理する。
- [x] 2.2 Enter で改行、Ctrl+Enter で送信となるキーボード操作を実装する。
- [x] 2.3 入力が空文字のときは送信処理を実行しないガードを実装する。

## 3. フロントエンド: 送信後のタイムライン反映

- [x] 3.1 `POST /api/messages` 呼び出しを実装し、送信中の多重送信を防止する。
- [x] 3.2 送信成功時に入力欄をクリアし、レスポンスのメッセージをタイムラインへ追加表示する。
- [x] 3.3 送信成功後にタイムラインを最下部へスクロールする処理を追加する。
- [x] 3.4 送信失敗時に入力内容を保持し、再送可能なエラー表示を行う。

## 4. スタイル調整と検証

- [x] 4.1 `App.css` を更新し、入力エリアと送信ボタンがスマホ/PC の両方で崩れないことを担保する。
- [x] 4.2 バックエンドの API テストまたは同等の確認で 201/400/401 の主要ケースを検証する。
- [x] 4.3 フロントエンド動作確認で「空送信不可」「送信後クリア」「送信後最下部スクロール」を確認する。
