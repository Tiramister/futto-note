## 1. バックエンド: メッセージ一覧 API

- [x] 1.1 `GET /api/messages` ハンドラーを追加する（認証ミドルウェア配下で `user_id` を取得）
- [x] 1.2 `messages` テーブルから `id, body, created_at` を `created_at ASC` で取得するクエリを実装する
- [x] 1.3 レスポンスを `{ "messages": [...] }` 形式で返却し、0 件時は空配列を返す
- [x] 1.4 認証なしアクセスで 401 を返すことを確認する

## 2. フロントエンド: タイムライン表示の基盤

- [x] 2.1 メイン画面表示時に `/api/messages` を取得する処理を追加する
- [x] 2.2 メッセージリスト領域と下部入力エリアを持つレイアウトへ更新する
- [x] 2.3 取得したメッセージを本文・投稿時刻付きで時系列表示する

## 3. フロントエンド: URL リンク化とスクロール

- [x] 3.1 メッセージ本文中の URL をクリック可能リンクとして描画する処理を実装する
- [x] 3.2 URL を含まない本文が通常テキストとして表示されることを確認する
- [x] 3.3 初回描画完了時にタイムラインを最下部へスクロールする処理を実装する

## 4. フロントエンド: レスポンシブ調整

- [x] 4.1 `App.css` にスマホ幅向けのメディアクエリを追加し、本文・時刻・入力エリアの可読性を確保する
- [x] 4.2 PC 幅での余白・行間・配置を調整し、一覧の視認性を確保する
- [x] 4.3 メッセージリスト領域が上方向スクロール可能であることを確認する

## 5. 動作確認

- [x] 5.1 DB にテストメッセージを登録し、一覧が表示されることを確認する
- [x] 5.2 URL を含むメッセージがリンクとしてクリック可能であることを確認する
- [x] 5.3 スマホ幅・PC幅の両方でレイアウトが崩れないことを確認する
